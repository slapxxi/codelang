The app code for the [Codelang](https://github.com/slapxxi/codelang).

Use relative paths (`~/`) in the `import` statements for convenience:

```ts
import '~/app/styles/app.css';
import { Button } from '~/ui';
// etc.
```
